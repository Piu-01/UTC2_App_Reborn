<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml
    Cấu trúc: Fragment container chiếm toàn bộ màn hình, LiquidBar đè lên phía dưới.
    Fragment container sẽ có padding-bottom = chiều cao LiquidBar (~60dp)
    để nội dung không bị che khuất.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- ── Fragment container ── -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="64dp"/>

    <!-- ── LiquidBar (Compose) – pinned to bottom ── -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/bottom_bar_compose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
