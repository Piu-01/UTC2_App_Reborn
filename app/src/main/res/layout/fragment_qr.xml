<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_qr.xml  — FIXED
    ────────────────────────────────────────────────────────
    Fixes applied:
      1. Root uses android:fitsSystemWindows="true" so the layout
         starts BELOW the status bar — toolbar no longer overlaps.
      2. All button colors are hardcoded via backgroundTint /
         strokeColor / textColor so they look identical on every
         Android version and screen density.
      3. Toolbar touch-target is 48dp min-height (Material guideline).

    View Binding class: FragmentQrBinding
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:fitsSystemWindows="true">

    <!--
         ══════════════════════════════════════════
         TOP BAR — back arrow + title
         Height 56dp, receives status-bar top-inset
         via QrFragment.applyWindowInsets()
         ══════════════════════════════════════════
    -->
    <LinearLayout
        android:id="@+id/toolbar_container"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="4dp"
        android:paddingEnd="16dp"
        android:background="@color/white">

        <!--
             Use a plain ImageButton with a guaranteed minimum
             touch area (48 × 48 dp) so it registers on all
             emulators and real devices.
        -->
        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:padding="8dp"
            android:contentDescription="Quay lại"
            android:scaleType="centerInside" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="Mã QR Sinh viên"
            android:textColor="@color/text_primary"
            android:textSize="18sp"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Scrollable body -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- ══════════════════════════════════
                 STUDENT NAME + MSSV
                 ══════════════════════════════════ -->
            <TextView
                android:id="@+id/tv_qr_student_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="32dp"
                android:textColor="@color/text_primary"
                android:textSize="22sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_qr_student_code"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="4dp"
                android:textColor="@color/text_secondary"
                android:textSize="14sp" />

            <!-- ══════════════════════════════════
                 QR CODE CARD
                 ══════════════════════════════════ -->
            <androidx.cardview.widget.CardView
                android:layout_width="280dp"
                android:layout_height="280dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="28dp"
                app:cardCornerRadius="20dp"
                app:cardElevation="8dp"
                app:cardBackgroundColor="@color/white">

                <ImageView
                    android:id="@+id/iv_qr_code"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:padding="20dp"
                    android:scaleType="fitCenter"
                    android:contentDescription="Mã QR sinh viên" />

            </androidx.cardview.widget.CardView>

            <!-- ══════════════════════════════════
                 ACTION BUTTONS
                 Colors are hardcoded so they render
                 identically on all API levels / OEMs.
                 ══════════════════════════════════ -->

            <!-- 1. Sao chép — filled pill button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_copy_code"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginStart="24dp"
                android:layout_marginEnd="24dp"
                android:layout_marginTop="32dp"
                android:text="Sao chép mã số sinh viên"
                android:textColor="@color/white"
                android:textSize="15sp"
                android:textAllCaps="false"
                app:backgroundTint="@color/qr_btn_primary"
                app:cornerRadius="26dp"
                app:icon="@drawable/ic_copy"
                app:iconGravity="textStart"
                app:iconTint="@color/white" />

            <!-- 2. Lưu ảnh — outlined -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_save_qr"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginStart="24dp"
                android:layout_marginEnd="24dp"
                android:layout_marginTop="12dp"
                android:text="LƯU ẢNH MÃ QR"
                android:textColor="@color/qr_btn_primary"
                android:textSize="13sp"
                android:letterSpacing="0.08"
                app:strokeColor="@color/qr_btn_primary"
                app:strokeWidth="0dp"
                app:backgroundTint="@color/white"
                app:cornerRadius="26dp"
                app:icon="@drawable/ic_download"
                app:iconTint="@color/qr_btn_primary"
                app:iconGravity="textStart" />

            <!-- 3. Chia sẻ — outlined -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_share_qr"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginStart="24dp"
                android:layout_marginEnd="24dp"
                android:layout_marginTop="8dp"
                android:text="CHIA SẺ"
                android:textColor="@color/qr_btn_primary"
                android:textSize="13sp"
                android:letterSpacing="0.08"
                app:strokeColor="@color/qr_btn_primary"
                app:strokeWidth="0dp"
                app:backgroundTint="@color/white"
                app:cornerRadius="26dp"
                app:icon="@drawable/ic_share"
                app:iconTint="@color/qr_btn_primary"
                app:iconGravity="textStart" />

            <!-- ══════════════════════════════════
                 CAPTION
                 ══════════════════════════════════ -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="20dp"
                android:layout_marginBottom="48dp"
                android:text="Quét mã QR này để xác thực thông tin sinh viên"
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</LinearLayout>