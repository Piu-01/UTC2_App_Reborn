<!-- PATH: app/src/main/res/layout/item_assessment_criteria.xml -->

<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp"
    android:background="@android:color/white">

    <!-- Divider top -->
    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="0.5dp"
        android:background="#E0E0E0"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Tên tiêu chí -->
    <TextView
        android:id="@+id/tvCriteriaTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="#222222"
        android:lineSpacingExtra="3dp"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tilScore"/>

    <!-- Điểm tối đa -->
    <TextView
        android:id="@+id/tvMaxScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="#888888"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvCriteriaTitle"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Nút nộp minh chứng (chỉ hiện khi requiresEvidence = true) -->
    <Button
        android:id="@+id/btnEvidence"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:text="Nộp MC"
        android:textSize="11sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:paddingHorizontal="10dp"
        android:stateListAnimator="@null"
        android:visibility="gone"
        app:strokeColor="@android:color/black"
        app:layout_constraintTop_toBottomOf="@id/tvMaxScore"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- Ô nhập điểm (dropdown + tay) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilScore"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
        android:layout_width="88dp"
        android:layout_height="wrap_content"
        android:hint="Điểm"
        app:boxStrokeColor="@android:color/black"
        app:hintTextColor="#666"
        app:layout_constraintTop_toBottomOf="@id/divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <com.google.android.material.textfield.MaterialAutoCompleteTextView
            android:id="@+id/actvScore"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#111"
            android:textStyle="bold"
            android:gravity="center"
            android:inputType="numberDecimal|numberSigned"/>

    </com.google.android.material.textfield.TextInputLayout>

</androidx.constraintlayout.widget.ConstraintLayout>