<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.UTC2AppReborn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Edge-to-edge: status bar transparent -->
    </style>

    <style name="Theme.UTC2AppReborn" parent="Base.Theme.UTC2AppReborn"/>

    <!--
        TabTextAppearance — dùng cho TabLayout nếu vẫn cần ở nơi khác
    -->
    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">13sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="textAllCaps">false</item>
    </style>

    <!--
        Chip text color: trắng khi selected, đen khi unselected.
        Áp dụng qua app:textColor trên từng Chip nếu cần override.
    -->
    <style name="FilterChipStyle" parent="Widget.Material3.Chip.Filter">
        <item name="android:textSize">13sp</item>
        <item name="android:minHeight">48dp</item>
        <item name="chipBackgroundColor">@color/chip_background_selector</item>
        <item name="android:textColor">@color/chip_text_selector</item>
    </style>

</resources>
