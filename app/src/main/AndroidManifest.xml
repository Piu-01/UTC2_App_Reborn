<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — UPDATED
    ──────────────────────────────────────────────────────────────
    Thêm <queries> để fix nút "Xem thêm thông báo" trên Android 11+.

    Android 11 (API 30) áp dụng Package Visibility Restrictions.
    Nếu không khai báo <queries>, intent ACTION_VIEW với scheme
    "https" sẽ không tìm được browser → startActivity() fail.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ════════════════════════════════════════════════════════
         FIX Android 11+: khai báo intent scheme cần dùng.
         Đặt TRƯỚC thẻ <application>.
         ════════════════════════════════════════════════════════ -->
    <queries>
        <!-- Cho phép mở URL https:// bằng browser bất kỳ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Cho phép mở URL http:// (fallback) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.UTC2AppReborn"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        tools:targetApi="28">

        <!-- LOGIN -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- NEWS DETAIL -->
        <activity
            android:name=".ui.news.NewsDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- FileProvider — Share QR -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>